#!/bin/sh

brightness=$(cat /sys/class/backlight/intel_backlight/max_brightness)

if (($brightness > 0)); then
  let brightness=$brightness-1413
    echo "echo $brightness > /sys/class/backlight/intel_backlight/brightness" | sudo zsh
fi
